// GENERATED CODE - DO NOT MODIFY BY HAND
// Generated by flutter_goldgen

import 'package:flutter_test/flutter_test.dart';
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:mocktail/mocktail.dart';
import 'package:achaean_flutter/design_system/theme/app_theme.dart';
import 'package:achaean_flutter/l10n/app_localizations.dart';
import 'package:achaean_flutter/features/post_creation/screens/own_posts_screen.dart';
import 'package:achaean_flutter/features/post_creation/cubit/own_posts_state.dart';
import '../fixtures/own_posts_state_fixtures.dart';

class MockOwnPostsCubit extends Mock implements OwnPostsCubit {
  MockOwnPostsCubit(this._state);
  final OwnPostsState _state;

  @override
  OwnPostsState get state => _state;

  @override
  Stream<OwnPostsState> get stream => Stream.value(_state);

  @override
  Future<void> close() async {}
}

void main() {
  // Vary OwnPostsCubit states, hold others at default
  for (var i = 0; i < OwnPostsStateFixtures.all.length; i++) {
    final fixture = OwnPostsStateFixtures.all[i];
    final label = OwnPostsStateFixtures.labels[i];
    for (final status in UiFlowStatus.values) {
      testWidgets(
        'own_posts_screen_own_posts_state_${status.name}_$label',
        (tester) async {
          tester.view.physicalSize = const Size(375, 812);
          tester.view.devicePixelRatio = 1.0;

          await tester.pumpWidget(
            MaterialApp(
              theme: AppTheme.lightTheme,
              locale: const Locale('en'),
              localizationsDelegates: AppLocalizations.localizationsDelegates,
              supportedLocales: AppLocalizations.supportedLocales,
              home: BlocProvider<OwnPostsCubit>.value(
                value: MockOwnPostsCubit(
                  fixture.copyWith(status: status, error: status == UiFlowStatus.failure ? Exception('Test error') : null),
                ),
                child: const OwnPostsScreen(),
              ),
            ),
          );
          await tester.pumpAndSettle();

          await expectLater(
            find.byType(OwnPostsScreen),
            matchesGoldenFile('phoneSE_en/own_posts_screen_own_posts_state_${status.name}_$label.png'),
          );

          addTearDown(() => tester.view.resetPhysicalSize());
        },
      );
    }
  }
  for (var i = 0; i < OwnPostsStateFixtures.all.length; i++) {
    final fixture = OwnPostsStateFixtures.all[i];
    final label = OwnPostsStateFixtures.labels[i];
    for (final status in UiFlowStatus.values) {
      testWidgets(
        'own_posts_screen_own_posts_state_${status.name}_$label',
        (tester) async {
          tester.view.physicalSize = const Size(430, 932);
          tester.view.devicePixelRatio = 1.0;

          await tester.pumpWidget(
            MaterialApp(
              theme: AppTheme.lightTheme,
              locale: const Locale('en'),
              localizationsDelegates: AppLocalizations.localizationsDelegates,
              supportedLocales: AppLocalizations.supportedLocales,
              home: BlocProvider<OwnPostsCubit>.value(
                value: MockOwnPostsCubit(
                  fixture.copyWith(status: status, error: status == UiFlowStatus.failure ? Exception('Test error') : null),
                ),
                child: const OwnPostsScreen(),
              ),
            ),
          );
          await tester.pumpAndSettle();

          await expectLater(
            find.byType(OwnPostsScreen),
            matchesGoldenFile('phoneMax_en/own_posts_screen_own_posts_state_${status.name}_$label.png'),
          );

          addTearDown(() => tester.view.resetPhysicalSize());
        },
      );
    }
  }
  for (var i = 0; i < OwnPostsStateFixtures.all.length; i++) {
    final fixture = OwnPostsStateFixtures.all[i];
    final label = OwnPostsStateFixtures.labels[i];
    for (final status in UiFlowStatus.values) {
      testWidgets(
        'own_posts_screen_own_posts_state_${status.name}_$label',
        (tester) async {
          tester.view.physicalSize = const Size(768, 1024);
          tester.view.devicePixelRatio = 1.0;

          await tester.pumpWidget(
            MaterialApp(
              theme: AppTheme.lightTheme,
              locale: const Locale('en'),
              localizationsDelegates: AppLocalizations.localizationsDelegates,
              supportedLocales: AppLocalizations.supportedLocales,
              home: BlocProvider<OwnPostsCubit>.value(
                value: MockOwnPostsCubit(
                  fixture.copyWith(status: status, error: status == UiFlowStatus.failure ? Exception('Test error') : null),
                ),
                child: const OwnPostsScreen(),
              ),
            ),
          );
          await tester.pumpAndSettle();

          await expectLater(
            find.byType(OwnPostsScreen),
            matchesGoldenFile('tablet_en/own_posts_screen_own_posts_state_${status.name}_$label.png'),
          );

          addTearDown(() => tester.view.resetPhysicalSize());
        },
      );
    }
  }
}
