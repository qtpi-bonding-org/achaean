// GENERATED CODE - DO NOT MODIFY BY HAND
// Generated by flutter_goldgen

import 'package:flutter_test/flutter_test.dart';
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:mocktail/mocktail.dart';
import 'package:achaean_flutter/design_system/theme/app_theme.dart';
import 'package:achaean_flutter/l10n/app_localizations.dart';
import 'package:achaean_flutter/features/account_creation/screens/account_creation_screen.dart';
import 'package:achaean_flutter/features/account_creation/cubit/account_creation_state.dart';
import '../fixtures/account_creation_state_fixtures.dart';

class MockAccountCreationCubit extends Mock implements AccountCreationCubit {
  MockAccountCreationCubit(this._state);
  final AccountCreationState _state;

  @override
  AccountCreationState get state => _state;

  @override
  Stream<AccountCreationState> get stream => Stream.value(_state);

  @override
  Future<void> close() async {}
}

void main() {
  // Vary AccountCreationCubit states, hold others at default
  for (var i = 0; i < AccountCreationStateFixtures.all.length; i++) {
    final fixture = AccountCreationStateFixtures.all[i];
    final label = AccountCreationStateFixtures.labels[i];
    for (final status in UiFlowStatus.values) {
      testWidgets(
        'account_creation_screen_account_creation_state_${status.name}_$label',
        (tester) async {
          tester.view.physicalSize = const Size(375, 812);
          tester.view.devicePixelRatio = 1.0;

          await tester.pumpWidget(
            MaterialApp(
              theme: AppTheme.lightTheme,
              locale: const Locale('en'),
              localizationsDelegates: AppLocalizations.localizationsDelegates,
              supportedLocales: AppLocalizations.supportedLocales,
              home: BlocProvider<AccountCreationCubit>.value(
                value: MockAccountCreationCubit(
                  fixture.copyWith(status: status, error: status == UiFlowStatus.failure ? Exception('Test error') : null),
                ),
                child: const AccountCreationScreen(),
              ),
            ),
          );
          await tester.pumpAndSettle();

          await expectLater(
            find.byType(AccountCreationScreen),
            matchesGoldenFile('phoneSE_en/account_creation_screen_account_creation_state_${status.name}_$label.png'),
          );

          // Semantics overlay golden
          await tester.pumpWidget(
            SemanticsDebugger(
              child: MaterialApp(
                theme: AppTheme.lightTheme,
                locale: const Locale('en'),
                localizationsDelegates: AppLocalizations.localizationsDelegates,
                supportedLocales: AppLocalizations.supportedLocales,
                home: BlocProvider<AccountCreationCubit>.value(
                  value: MockAccountCreationCubit(
                    fixture.copyWith(status: status, error: status == UiFlowStatus.failure ? Exception('Test error') : null),
                  ),
                  child: const AccountCreationScreen(),
                ),
              ),
            ),
          );
          await tester.pumpAndSettle();

          await expectLater(
            find.byType(SemanticsDebugger),
            matchesGoldenFile('phoneSE_en/semantics/account_creation_screen_account_creation_state_${status.name}_$label.png'),
          );

          addTearDown(() => tester.view.resetPhysicalSize());
        },
      );
    }
  }
  for (var i = 0; i < AccountCreationStateFixtures.all.length; i++) {
    final fixture = AccountCreationStateFixtures.all[i];
    final label = AccountCreationStateFixtures.labels[i];
    for (final status in UiFlowStatus.values) {
      testWidgets(
        'account_creation_screen_account_creation_state_${status.name}_$label',
        (tester) async {
          tester.view.physicalSize = const Size(430, 932);
          tester.view.devicePixelRatio = 1.0;

          await tester.pumpWidget(
            MaterialApp(
              theme: AppTheme.lightTheme,
              locale: const Locale('en'),
              localizationsDelegates: AppLocalizations.localizationsDelegates,
              supportedLocales: AppLocalizations.supportedLocales,
              home: BlocProvider<AccountCreationCubit>.value(
                value: MockAccountCreationCubit(
                  fixture.copyWith(status: status, error: status == UiFlowStatus.failure ? Exception('Test error') : null),
                ),
                child: const AccountCreationScreen(),
              ),
            ),
          );
          await tester.pumpAndSettle();

          await expectLater(
            find.byType(AccountCreationScreen),
            matchesGoldenFile('phoneMax_en/account_creation_screen_account_creation_state_${status.name}_$label.png'),
          );

          // Semantics overlay golden
          await tester.pumpWidget(
            SemanticsDebugger(
              child: MaterialApp(
                theme: AppTheme.lightTheme,
                locale: const Locale('en'),
                localizationsDelegates: AppLocalizations.localizationsDelegates,
                supportedLocales: AppLocalizations.supportedLocales,
                home: BlocProvider<AccountCreationCubit>.value(
                  value: MockAccountCreationCubit(
                    fixture.copyWith(status: status, error: status == UiFlowStatus.failure ? Exception('Test error') : null),
                  ),
                  child: const AccountCreationScreen(),
                ),
              ),
            ),
          );
          await tester.pumpAndSettle();

          await expectLater(
            find.byType(SemanticsDebugger),
            matchesGoldenFile('phoneMax_en/semantics/account_creation_screen_account_creation_state_${status.name}_$label.png'),
          );

          addTearDown(() => tester.view.resetPhysicalSize());
        },
      );
    }
  }
  for (var i = 0; i < AccountCreationStateFixtures.all.length; i++) {
    final fixture = AccountCreationStateFixtures.all[i];
    final label = AccountCreationStateFixtures.labels[i];
    for (final status in UiFlowStatus.values) {
      testWidgets(
        'account_creation_screen_account_creation_state_${status.name}_$label',
        (tester) async {
          tester.view.physicalSize = const Size(768, 1024);
          tester.view.devicePixelRatio = 1.0;

          await tester.pumpWidget(
            MaterialApp(
              theme: AppTheme.lightTheme,
              locale: const Locale('en'),
              localizationsDelegates: AppLocalizations.localizationsDelegates,
              supportedLocales: AppLocalizations.supportedLocales,
              home: BlocProvider<AccountCreationCubit>.value(
                value: MockAccountCreationCubit(
                  fixture.copyWith(status: status, error: status == UiFlowStatus.failure ? Exception('Test error') : null),
                ),
                child: const AccountCreationScreen(),
              ),
            ),
          );
          await tester.pumpAndSettle();

          await expectLater(
            find.byType(AccountCreationScreen),
            matchesGoldenFile('tablet_en/account_creation_screen_account_creation_state_${status.name}_$label.png'),
          );

          // Semantics overlay golden
          await tester.pumpWidget(
            SemanticsDebugger(
              child: MaterialApp(
                theme: AppTheme.lightTheme,
                locale: const Locale('en'),
                localizationsDelegates: AppLocalizations.localizationsDelegates,
                supportedLocales: AppLocalizations.supportedLocales,
                home: BlocProvider<AccountCreationCubit>.value(
                  value: MockAccountCreationCubit(
                    fixture.copyWith(status: status, error: status == UiFlowStatus.failure ? Exception('Test error') : null),
                  ),
                  child: const AccountCreationScreen(),
                ),
              ),
            ),
          );
          await tester.pumpAndSettle();

          await expectLater(
            find.byType(SemanticsDebugger),
            matchesGoldenFile('tablet_en/semantics/account_creation_screen_account_creation_state_${status.name}_$label.png'),
          );

          addTearDown(() => tester.view.resetPhysicalSize());
        },
      );
    }
  }
}
