// GENERATED CODE - DO NOT MODIFY BY HAND
// Generated by flutter_goldgen

import 'package:flutter_test/flutter_test.dart';
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:mocktail/mocktail.dart';
import 'package:achaean_flutter/design_system/theme/app_theme.dart';
import 'package:achaean_flutter/l10n/app_localizations.dart';
import 'package:achaean_flutter/features/personal_feed/screens/personal_feed_screen.dart';
import 'package:achaean_flutter/features/personal_feed/cubit/personal_feed_state.dart';
import '../fixtures/personal_feed_state_fixtures.dart';

class MockPersonalFeedCubit extends Mock implements PersonalFeedCubit {
  MockPersonalFeedCubit(this._state);
  final PersonalFeedState _state;

  @override
  PersonalFeedState get state => _state;

  @override
  Stream<PersonalFeedState> get stream => Stream.value(_state);

  @override
  Future<void> close() async {}
}

void main() {
  // Vary PersonalFeedCubit states, hold others at default
  for (var i = 0; i < PersonalFeedStateFixtures.all.length; i++) {
    final fixture = PersonalFeedStateFixtures.all[i];
    final label = PersonalFeedStateFixtures.labels[i];
    for (final status in UiFlowStatus.values) {
      testWidgets(
        'personal_feed_screen_personal_feed_state_${status.name}_$label',
        (tester) async {
          tester.view.physicalSize = const Size(375, 812);
          tester.view.devicePixelRatio = 1.0;

          await tester.pumpWidget(
            MaterialApp(
              theme: AppTheme.lightTheme,
              locale: const Locale('en'),
              localizationsDelegates: AppLocalizations.localizationsDelegates,
              supportedLocales: AppLocalizations.supportedLocales,
              home: BlocProvider<PersonalFeedCubit>.value(
                value: MockPersonalFeedCubit(
                  fixture.copyWith(status: status, error: status == UiFlowStatus.failure ? Exception('Test error') : null),
                ),
                child: const PersonalFeedScreen(),
              ),
            ),
          );
          await tester.pumpAndSettle();

          await expectLater(
            find.byType(PersonalFeedScreen),
            matchesGoldenFile('phoneSE_en/personal_feed_screen_personal_feed_state_${status.name}_$label.png'),
          );

          // Semantics overlay golden
          await tester.pumpWidget(
            SemanticsDebugger(
              child: MaterialApp(
                theme: AppTheme.lightTheme,
                locale: const Locale('en'),
                localizationsDelegates: AppLocalizations.localizationsDelegates,
                supportedLocales: AppLocalizations.supportedLocales,
                home: BlocProvider<PersonalFeedCubit>.value(
                  value: MockPersonalFeedCubit(
                    fixture.copyWith(status: status, error: status == UiFlowStatus.failure ? Exception('Test error') : null),
                  ),
                  child: const PersonalFeedScreen(),
                ),
              ),
            ),
          );
          await tester.pumpAndSettle();

          await expectLater(
            find.byType(SemanticsDebugger),
            matchesGoldenFile('phoneSE_en/semantics/personal_feed_screen_personal_feed_state_${status.name}_$label.png'),
          );

          addTearDown(() => tester.view.resetPhysicalSize());
        },
      );
    }
  }
  for (var i = 0; i < PersonalFeedStateFixtures.all.length; i++) {
    final fixture = PersonalFeedStateFixtures.all[i];
    final label = PersonalFeedStateFixtures.labels[i];
    for (final status in UiFlowStatus.values) {
      testWidgets(
        'personal_feed_screen_personal_feed_state_${status.name}_$label',
        (tester) async {
          tester.view.physicalSize = const Size(430, 932);
          tester.view.devicePixelRatio = 1.0;

          await tester.pumpWidget(
            MaterialApp(
              theme: AppTheme.lightTheme,
              locale: const Locale('en'),
              localizationsDelegates: AppLocalizations.localizationsDelegates,
              supportedLocales: AppLocalizations.supportedLocales,
              home: BlocProvider<PersonalFeedCubit>.value(
                value: MockPersonalFeedCubit(
                  fixture.copyWith(status: status, error: status == UiFlowStatus.failure ? Exception('Test error') : null),
                ),
                child: const PersonalFeedScreen(),
              ),
            ),
          );
          await tester.pumpAndSettle();

          await expectLater(
            find.byType(PersonalFeedScreen),
            matchesGoldenFile('phoneMax_en/personal_feed_screen_personal_feed_state_${status.name}_$label.png'),
          );

          // Semantics overlay golden
          await tester.pumpWidget(
            SemanticsDebugger(
              child: MaterialApp(
                theme: AppTheme.lightTheme,
                locale: const Locale('en'),
                localizationsDelegates: AppLocalizations.localizationsDelegates,
                supportedLocales: AppLocalizations.supportedLocales,
                home: BlocProvider<PersonalFeedCubit>.value(
                  value: MockPersonalFeedCubit(
                    fixture.copyWith(status: status, error: status == UiFlowStatus.failure ? Exception('Test error') : null),
                  ),
                  child: const PersonalFeedScreen(),
                ),
              ),
            ),
          );
          await tester.pumpAndSettle();

          await expectLater(
            find.byType(SemanticsDebugger),
            matchesGoldenFile('phoneMax_en/semantics/personal_feed_screen_personal_feed_state_${status.name}_$label.png'),
          );

          addTearDown(() => tester.view.resetPhysicalSize());
        },
      );
    }
  }
  for (var i = 0; i < PersonalFeedStateFixtures.all.length; i++) {
    final fixture = PersonalFeedStateFixtures.all[i];
    final label = PersonalFeedStateFixtures.labels[i];
    for (final status in UiFlowStatus.values) {
      testWidgets(
        'personal_feed_screen_personal_feed_state_${status.name}_$label',
        (tester) async {
          tester.view.physicalSize = const Size(768, 1024);
          tester.view.devicePixelRatio = 1.0;

          await tester.pumpWidget(
            MaterialApp(
              theme: AppTheme.lightTheme,
              locale: const Locale('en'),
              localizationsDelegates: AppLocalizations.localizationsDelegates,
              supportedLocales: AppLocalizations.supportedLocales,
              home: BlocProvider<PersonalFeedCubit>.value(
                value: MockPersonalFeedCubit(
                  fixture.copyWith(status: status, error: status == UiFlowStatus.failure ? Exception('Test error') : null),
                ),
                child: const PersonalFeedScreen(),
              ),
            ),
          );
          await tester.pumpAndSettle();

          await expectLater(
            find.byType(PersonalFeedScreen),
            matchesGoldenFile('tablet_en/personal_feed_screen_personal_feed_state_${status.name}_$label.png'),
          );

          // Semantics overlay golden
          await tester.pumpWidget(
            SemanticsDebugger(
              child: MaterialApp(
                theme: AppTheme.lightTheme,
                locale: const Locale('en'),
                localizationsDelegates: AppLocalizations.localizationsDelegates,
                supportedLocales: AppLocalizations.supportedLocales,
                home: BlocProvider<PersonalFeedCubit>.value(
                  value: MockPersonalFeedCubit(
                    fixture.copyWith(status: status, error: status == UiFlowStatus.failure ? Exception('Test error') : null),
                  ),
                  child: const PersonalFeedScreen(),
                ),
              ),
            ),
          );
          await tester.pumpAndSettle();

          await expectLater(
            find.byType(SemanticsDebugger),
            matchesGoldenFile('tablet_en/semantics/personal_feed_screen_personal_feed_state_${status.name}_$label.png'),
          );

          addTearDown(() => tester.view.resetPhysicalSize());
        },
      );
    }
  }
}
