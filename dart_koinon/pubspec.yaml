name: dart_koinon
description: Koinon protocol primitives — models, enums, and repo scaffolding.

environment:
  sdk: '^3.8.0'

resolution: workspace

dependencies:
  dart_git:
    path: ../dart_git
  freezed_annotation: ^3.1.0
  json_annotation: ^4.11.0

dev_dependencies:
  build_runner: ^2.4.0
  freezed: ^3.2.5
  json_serializable: ^6.13.1
  lints: '>=3.0.0 <7.0.0'
  test: ^1.25.5
