name: achaean_server
description: Starting point for a Serverpod server.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '^3.8.0'

resolution: workspace

dependencies:
  serverpod: 3.4.1
  serverpod_auth_idp_server: 3.4.1
  dart_git:
    path: ../dart_git
  dart_koinon:
    path: ../dart_koinon
  anonaccount_server:
    git:
      url: https://github.com/qtpi-bonding-org/serverpod_anonaccred.git
      ref: 5f364aee45cb6a39e7f07b7aaf8187a5164d3f88
      path: anonaccount_server
  pointycastle: ^4.0.0

dev_dependencies:
  lints: '>=3.0.0 <7.0.0'
  serverpod_test: 3.4.1
  test: ^1.25.5

serverpod:
  scripts:
    # Starts the server and applies migrations
    start: dart bin/main.dart --apply-migrations

    # Build the Flutter web app and copy it to the server's web directory.
    # Windows uses xcopy since flutter's --output flag doesn't work there.
    # See: https://github.com/flutter/flutter/issues/157886
    flutter_build:
      windows: >-
        (if exist web\app rmdir /S /Q web\app)
        & cd /D ..\achaean_flutter
        && flutter build web --base-href /app/ --wasm
        && xcopy /E /I /Y build\web ..\achaean_server\web\app
      posix: |
        cd ../achaean_flutter
        flutter build web --base-href /app/ --wasm --output ../achaean_server/web/app
