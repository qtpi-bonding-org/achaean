name: dart_git
description: Abstract Git client with Forgejo implementation.

environment:
  sdk: '^3.8.0'

resolution: workspace

dependencies:
  http: ^1.2.0
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.4.0
  json_serializable: ^6.7.0
  lints: '>=3.0.0 <7.0.0'
  test: ^1.25.5
